import { create } from "@storybook/theming/create";

export default create({
  base: "light",
  brandTitle: "RFS UI",
  brandUrl: "/",
  brandImage: "/public/logo_rfsmart.png",
  brandTarget: "_self",
});
